# Web layer — pinned to match the sibling api-microservice.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# Inference stack. Left as lower bounds so the platform-appropriate torch build
# that ultralytics depends on resolves cleanly (CPU or CUDA). Tested against
# ultralytics 8.4.x / torch 2.x / numpy 1.26+ / pillow 10+.
#
# torch itself is intentionally NOT listed here: the Dockerfile installs the
# CPU-only wheel explicitly (via --index-url https://download.pytorch.org/whl/cpu)
# before this file, since Cloud Run has no GPU and the default PyPI resolution
# would otherwise pull a multi-GB CUDA build. For local dev, `pip install -r
# requirements.txt` alone will pull whatever torch build PyPI resolves for you.
ultralytics>=8.3.0
pillow>=10.0.0
numpy>=1.26.0
