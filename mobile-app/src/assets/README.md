# Assets Folder

This folder contains all static assets for the JalanGuard mobile app:

- `/images` - App images and icons
- `/fonts` - Custom font files

## Usage

Import assets using the relative path:

```typescript
import Logo from "../assets/images/logo.png";
```
